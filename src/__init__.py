"""Import root for the Surrey biome pipeline.

Present so setuptools can discover ``src`` as a real package rather than relying
on PEP 420 namespace resolution, which only worked when the current working
directory happened to be the repo root.
"""
